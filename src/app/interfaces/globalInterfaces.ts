export interface OptionList {
    id: number;
    name: string;
    isHover: boolean;
    icon: string;
    listClassName: string;
}

export interface OptionValues {
    optionId:number, 
    newOptionValue:boolean
}